# 04 - Enum and Collections

Manipulação de coleções com o módulo `Enum` e estruturas de dados.

## Conteúdo

- `enum.exs` — operações com `Enum`: max, reduce, filter, map
- `lists.exs` — funções do módulo `List`
- `maps.exs` — atualização e acesso em maps
- `keywords.exs` — listas de keywords (keyword lists)
