# 06 - Streams with Files

Leitura lazy de arquivos usando `File.stream!` e `Stream`.

## Conteúdo

- `stream_file.exs` — leitura e impressão linha a linha de um arquivo usando streams
- `neo.txt` — arquivo de texto usado como entrada
